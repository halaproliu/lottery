import { SHOW_MESSAGE } from './actions'

const initialState = {
  message: '',
  animateClass: ''
}

const reducer = (state = initialState, action) => {
  // switch (action) {
  //   case SHOW_MESSAGE:

  // }
  return state
}

export default reducer